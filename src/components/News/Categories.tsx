import { Button } from "@mui/material";
import React from "react";



function Categories({value, categoryList, onClickCategory}) {
{/*
    const [activeIndex, setActiveIndex] = React.useState(0);
    const [subMenu, activeSubMenu] = React.useState(0)


  const choseCategory = (index) => {
    setActiveIndex(index);
  };
  
  return (
    
    <div  className="sc-press-center__page__categories">
      <ul>
        {categoryList.map((categoryName, i) => (
          
          <li 
            key={i} 
          >
            <Button onClick={() => onClickCategory(i)}
            className={value === i ? "btn fire" : "btn"}>
          
            <div className="btn__content">{categoryName}</div>
            
            </Button>
            
          
           </li>
         
         
        ))}
 

      </ul>
    </div>
  );
*/}
  }

export default  Categories;
